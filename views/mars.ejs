<!DOCTYPE html>
<html lang="en">
  <%- include('./layout'); %>
  <body>
    <div class="mars-container">
      <h1>From Mars!</h1>
      <div>
        <% for (var i =1; i <=10; i++ ) { %>
        <div class="marsImg"><img src="<%= images[i].img_src %>" /></div>
        <% } %>
      </div>
    </div>
  </body>
</html>
